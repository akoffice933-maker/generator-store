import LoginForm from "./LoginForm";

export const metadata = { title: "Вход и регистрация — Generator Store" };

export default function LoginPage() {
  return <main className="flex min-h-[70vh] items-center justify-center px-4 py-12"><LoginForm /></main>;
}
